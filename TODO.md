# TODO

- [ ] Improve cart UX: header hierarchy, card hover, buttons, quantity controls states.
- [ ] Ensure responsive cart layout looks good (mobile table->cards, summary sizing).
- [ ] Improve overall cart page UI/UX without breaking existing styling.
- [ ] Verify `src/App.css` hasn’t been accidentally corrupted (basic sanity check: CSS still loads).
- [ ] Test: run `npm run build` (or `npm run dev`) to ensure no regressions.

